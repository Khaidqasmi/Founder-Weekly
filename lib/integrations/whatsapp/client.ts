export function getWhatsAppClient(_accessToken: string, _phoneNumberId: string) {
  throw new Error('WhatsApp integration requires credentials. Set WHATSAPP_ACCESS_TOKEN and WHATSAPP_PHONE_NUMBER_ID.')
}
