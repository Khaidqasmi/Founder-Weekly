export async function syncShopifyInventory(_workspaceId: string, _shopDomain: string, _accessToken: string) {
  throw new Error('Shopify sync requires credentials. Set SHOPIFY_CLIENT_ID and SHOPIFY_CLIENT_SECRET.')
}
